import { StyleSheet } from "react-native";

export const BasicTextStyle = StyleSheet.create({
    container: {
        color: "white"
    }
});

export const BasicTextInputStyle = StyleSheet.create({
    input: {
        color: "white",
        fontFamily: "Poppins"
    }
});
